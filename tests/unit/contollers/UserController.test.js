/**
 * Created by nstyladmin on 30/10/2015.
 */
/**
 * Created by nstyladmin on 30/10/2015.
 */
nullcallback = function(parameter){}
var chai = require('chai');
var expect = chai.expect;
var should = chai.should;
var assert = chai.assert;

describe('UserContollerModel', function() {

  /**
   * Test find all users
   */
  describe('#find()', function () {
    it('should check find function', function (done) {

      // example of expect
      expect(4+4).to.equal(9);

      // call when done
      done();

    });
  });

  /**
   * TEst find user with id
   */
  describe('#find(id)', function () {
    it('should check find function with user id', function (done) {

      // example of expect
      expect(4+4).to.equal(9);

      // call when done
      done();

    });
  });

  /**
   * TEst ---------
   */
});

